# Переменные окружения

В этом справочнике описаны все переменные окружения, которые можно настроить в HomeoRemedyTest.

## Обязательные переменные

### `SECRET_KEY`
- **Описание:** Секретный ключ для Flask (шифрование сессий)
- **Формат:** Строка (минимум 32 символа)
- **По умолчанию:** `dev-secret-key-change-in-production`
- **Пример:**
  ```env
  SECRET_KEY=ваш_секретный_ключ_из_32_символов
  ```
  
#### Как получить:

```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

___
## STORAGE_MODE

* Описание: Режим хранения данных
* Допустимые значения:
    - local - только локальное хранение
    - yandex - только Яндекс.Диск
    - hybrid - гибридный режим (рекомендуется)

* По умолчанию: hybrid
* Пример:
    ```env
    STORAGE_MODE=hybrid
    ```
  
Настройки хранилища
YANDEX_DISK_TOKEN
Описание: OAuth токен для доступа к Яндекс.Диску

Требуется для режимов: yandex, hybrid

Формат: Строка

Как получить: https://yandex.ru/dev/disk/poligon/

Пример:

env
YANDEX_DISK_TOKEN=AgAAAAAXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
YANDEX_DISK_PATH
Описание: Путь к файлу данных на Яндекс.Диске

Формат: Путь в формате Яндекс.Диска

По умолчанию: test_cards.json

Пример:

env
YANDEX_DISK_PATH=HomeoRemedyTest/app/data/test_cards.json
LOCAL_DATA_PATH
Описание: Путь к локальному файлу данных

Формат: Относительный или абсолютный путь

По умолчанию: app/data/test_cards.json

Примеры:

env
# Относительный путь (от корня проекта)
LOCAL_DATA_PATH=app/data/test_cards.json

# Абсолютный путь
LOCAL_DATA_PATH=/home/user/data/test_cards.json

# На Vercel используйте /tmp
LOCAL_DATA_PATH=/tmp/test_cards.json
Настройки приложения
CARDS_PER_PAGE
Описание: Количество карточек на странице

Тип: Целое число

По умолчанию: 20

Пример:

env
CARDS_PER_PAGE=50
SEARCH_DELAY
Описание: Задержка поиска в миллисекундах

Тип: Целое число

По умолчанию: 500

Пример:

env
SEARCH_DELAY=300
Расширенные настройки
DEBUG
Описание: Режим отладки Flask

Допустимые значения: True, False

По умолчанию: False

Пример:

env
DEBUG=True
FLASK_ENV
Описание: Окружение Flask

Допустимые значения: development, production

По умолчанию: production

Пример:

env
FLASK_ENV=development
HOST
Описание: Хост для запуска сервера

По умолчанию: 127.0.0.1

Пример:

env
HOST=0.0.0.0
PORT
Описание: Порт для запуска сервера

По умолчанию: 5000

Пример:

env
PORT=5001
Настройки для Vercel
VERCEL
Описание: Флаг окружения Vercel

Автоматически устанавливается: 1

Не изменяйте вручную

PYTHONPATH
Описание: Путь к модулям Python

Пример для Vercel:

env
PYTHONPATH=/var/task/app
Настройки базы данных (будущие версии)
DATABASE_URL
Описание: URL для подключения к PostgreSQL

Планируется в версии: 3.0

Пример:

env
DATABASE_URL=postgresql://user:password@localhost/dbname
REDIS_URL
Описание: URL для подключения к Redis

Планируется в версии: 3.0

Пример:

env
REDIS_URL=redis://localhost:6379/0
Пример полного файла .env
env
# === БЕЗОПАСНОСТЬ ===
SECRET_KEY=ваш_секретный_ключ_из_32_символов

# === РЕЖИМ ХРАНЕНИЯ ===
STORAGE_MODE=hybrid

# === ЯНДЕКС.ДИСК ===
YANDEX_DISK_TOKEN=AgAAAAAXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
YANDEX_DISK_PATH=HomeoRemedyTest/app/data/test_cards.json

# === ЛОКАЛЬНОЕ ХРАНЕНИЕ ===
LOCAL_DATA_PATH=app/data/test_cards.json

# === НАСТРОЙКИ ПРИЛОЖЕНИЯ ===
CARDS_PER_PAGE=20
SEARCH_DELAY=500

# === ОКРУЖЕНИЕ ===
FLASK_ENV=production
DEBUG=False

# === СЕРВЕР ===
HOST=0.0.0.0
PORT=5001
Проверка переменных окружения
Команда для проверки
bash
# Вывести все переменные окружения
python -c "
import os
from config import Config

print('=' * 60)
print('ПРОВЕРКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ')
print('=' * 60)

# Обязательные
print(f'SECRET_KEY:       {\"установлен\" if Config.SECRET_KEY != \"dev-secret-key-change-in-production\" else \"используется дефолтный\"}')
print(f'STORAGE_MODE:     {Config.STORAGE_MODE}')
print(f'YANDEX_DISK_TOKEN: {\"установлен\" if Config.YANDEX_DISK_TOKEN else \"не установлен\"}')

# Настройки приложения
print(f'CARDS_PER_PAGE:   {Config.CARDS_PER_PAGE}')
print(f'SEARCH_DELAY:     {Config.SEARCH_DELAY}')

# Пути
print(f'LOCAL_DATA_PATH:  {Config.LOCAL_DATA_PATH}')
print(f'JSON_FILE:        {Config.JSON_FILE}')
print(f'JSON существует:  {Config.JSON_FILE.exists()}')

print('=' * 60)
"
Тестовый скрипт
Создайте файл check_env.py:

python
#!/usr/bin/env python3
"""Скрипт для проверки переменных окружения"""

import os
from dotenv import load_dotenv

load_dotenv()

required_vars = ['SECRET_KEY', 'STORAGE_MODE']
recommended_vars = ['YANDEX_DISK_TOKEN', 'YANDEX_DISK_PATH']

print("🔍 Проверка переменных окружения")
print("=" * 40)

all_ok = True

# Проверяем обязательные переменные
print("\n📋 Обязательные переменные:")
for var in required_vars:
    value = os.environ.get(var)
    if value:
        if var == 'SECRET_KEY':
            print(f"  ✅ {var}: установлен")
        else:
            print(f"  ✅ {var}: {value}")
    else:
        print(f"  ❌ {var}: не установлен")
        all_ok = False

# Проверяем рекомендуемые переменные
print("\n💡 Рекомендуемые переменные:")
for var in recommended_vars:
    value = os.environ.get(var)
    if value:
        print(f"  ✅ {var}: установлен")
    else:
        print(f"  ⚠️  {var}: не установлен (требуется для облачной синхронизации)")

print("\n" + "=" * 40)
if all_ok:
    print("✅ Все обязательные переменные установлены")
else:
    print("❌ Не все обязательные переменные установлены")
    print("   Исправьте .env файл и перезапустите приложение")
Безопасность переменных окружения
Не храните в .env
❌ Никогда не храните в .env:

Пароли в открытом виде

Ключи доступа к продакшен-базам

Личные данные пользователей

Используйте секреты
✅ Рекомендуемые практики:

Добавьте .env в .gitignore

Используйте .env.example для шаблона

Для продакшена используйте:

GitHub Secrets

Vercel Environment Variables

Docker Secrets

HashiCorp Vault

Ротация ключей
bash
# Скрипт для ротации SECRET_KEY
python -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))"
Переменные для разработки
Файл .env.development
env
# Разработка
DEBUG=True
FLASK_ENV=development

# Хранилище
STORAGE_MODE=local

# База данных
DATABASE_URL=sqlite:///dev.db
Файл .env.test
env
# Тестирование
DEBUG=True
FLASK_ENV=testing

# Хранилище
STORAGE_MODE=local
LOCAL_DATA_PATH=/tmp/test_cards_test.json

# База данных
DATABASE_URL=sqlite:///:memory:
Файл .env.production
env
# Продакшен
DEBUG=False
FLASK_ENV=production

# Хранилище
STORAGE_MODE=hybrid
YANDEX_DISK_TOKEN=${PRODUCTION_YANDEX_TOKEN}

# Безопасность
SECRET_KEY=${PRODUCTION_SECRET_KEY}
Загрузка переменных окружения
Порядок загрузки
Системные переменные окружения

Файл .env (если существует)

Файл .env.local (если существует, переопределяет .env)

Файл .env.${FLASK_ENV} (если существует)

Пример загрузки
python
from dotenv import load_dotenv
import os

# Загружаем .env
load_dotenv()

# Или явно указываем путь
load_dotenv('.env.production')

# Получаем переменную
token = os.environ.get('YANDEX_DISK_TOKEN')
Устранение неполадок
Проблема: Переменные не загружаются
Решение:

Проверьте, что файл .env в корне проекта

Убедитесь, что файл имеет правильный формат

Перезапустите приложение

Проблема: Конфликт переменных
Решение:

Проверьте дублирование в .env файле

Удалите лишние пробелы вокруг =

Используйте export для системных переменных

Проблема: Специальные символы
Решение: Экранируйте специальные символы:

env
# Неправильно
PASSWORD=my@password#123

# Правильно
PASSWORD="my@password#123"
Ресурсы
Документация python-dotenv

Flask Configuration

12-Factor App: Config

Vercel Environment Variables
___
